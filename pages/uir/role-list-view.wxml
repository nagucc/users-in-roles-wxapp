<template name="role-list-view">
  <view wx:if="{{roles.length}}">
    <view class="weui-cells__title">Roles</view>
    <view class="weui-cells weui-cells_after-title">
      <view class="weui-cell" wx:for="{{roles}}" wx:key="{{*item}}">
        <view class="weui-cell__bd">{{item}}</view>
      </view>
    </view>
  </view>
</template>